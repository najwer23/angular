## User Table - Angular + WebSockets

A full-stack application displaying a filterable, sortable, and paginated user table synced in real time via WebSockets. State management was initially built with NgRx (Redux), then refactored to a session storage service for a more pragmatic fit. Includes unit tests for the user listing component.
